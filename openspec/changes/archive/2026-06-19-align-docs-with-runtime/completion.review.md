# Completion Review

- Updated `openspec/TODO.md` so archived ingestion/runtime changes are not marked active and the remaining scanner/registry repository migration is tracked accurately.
- Refreshed repo-level `completion.review` to describe the current implemented runtime state.
- Updated `README.md` with the shipped production composition, metadata persistence, apply guard, scheduler `serve` entrypoint, empty pending allowlist, and current OpenSpec validation command.
- Verification: OpenSpec validation passed; full pytest passed.
